# Release SOP

Freeze → full validation → compatibility decision → release candidate → sample project validation → tag → packages → changelog → upgrade guide.

Engine API version, content schema version, and save schema version are separate. Breaking changes require an ADR and upgrade instructions.
