# ENG-010 validation record

- Story: ENG-010 Schema-driven command validation
- GitHub Issue: #9
- Status: In Progress
- Branch: `agent/ENG-010-schema-validation`
- Architecture: `docs/decisions/ADR-0015-SCHEMA-DRIVEN-COMMAND-VALIDATION.md`

## Acceptance matrix

| Requirement | Required evidence | Status |
| --- | --- | --- |
| Required fields and value kinds share one authority | 45 engine-schema unit tests; 9 engine-command tests | Pass |
| Bounds, enums, array constraints, nullability, and defaults are consistent | Round-trip, normalization, and invalid-input tests in engine_schema | Pass |
| FieldSchema constraints affect rendering and runtime validation | Golden schema tests (renders_*), validator tests (numeric/string/array constraints) | Pass |
| Invalid schema definitions fail registration | definition_rejects_* tests (7 tests covering incompatible-kind, contradictory-bounds, duplicate-field, invalid-default, nested) | Pass |
| Invalid command input has stable code and field path | missing_required_field_produces_stable_diagnostic, wrong_type_produces_stable_diagnostic, empty_path_rejected_structurally | Pass |
| Advertised schemas cannot drift from runtime validation | descriptor_input_schema_generated_from_authority test; registry auto-generates from schema | Pass |
| Built-in document commands use the shared validator | 9 engine-command tests including default normalization and structural failure atomicity | Pass |
| Tactical commands use the shared validator | tactical-rpg-tools tests pass with migrated schemas | Pass |
| Schema versions appear in capability output | CLI verification: all 8 commands have x-schema-version | Pass |
| Structural failure is atomic | structural_failure_does_not_consume_transaction test | Pass |
| Workbench survives invalid schema input | malformed_command_input_rejected_before_access_planning test; JSONL workbench integration tests pass | Pass |

## Targeted validation

```text
cargo test -p guiyi-engine-schema -> ok (45 tests passed)
cargo test -p guiyi-engine-command -> ok (9 tests passed)
cargo test -p guiyi-engine-agent-tools -> ok (3 tests passed)
cargo test -p guiyi-engine-agent-host -> ok (9 tests passed, including malformed input test)
cargo test -p tactical-rpg-tools -> ok (1 test passed)
cargo run -p guiyi-engine-cli -- capabilities --json -> ok (8 commands, all with x-schema-version)
```

## Full repository gates

```text
cargo fmt --all -- --check -> clean
cargo check --workspace --all-features -> ok
cargo clippy --workspace --all-targets --all-features -- -D warnings -> ok (zero warnings)
cargo test --workspace --all-features -> ok (118+ tests passed)
cargo doc --workspace --no-deps -> ok
python3 scripts/check_architecture.py -> passed (24 packages)
python3 scripts/check_docs.py -> passed
python3 scripts/validate_repository.py -> passed (24 workspace members)
python3 scripts/static_rust_check.py -> passed (29 files)
examples -> all 3 examples pass
```

## CI evidence

- Implementation CI run: Pending push
- Final clean-head CI run: Pending
- Required jobs: `rust`, `repository-gates`
- Accepted head SHA: Pending
- Squash merge SHA: Pending

## Compatibility and migration evidence

- Public dialect version: 1, emitted as `"x-schema-version": 1`
- Previously accepted command inputs remain valid (additionalProperties defaults to allowed)
- Default application: structural validation succeeds, then defaults applied before access planning, semantic validation, and apply
- Additional-property policy: allowed by default; commands may explicitly set `additionalProperties: false`
- Stable diagnostic codes: COMMAND_INPUT_REQUIRED, COMMAND_INPUT_TYPE_MISMATCH, COMMAND_INPUT_NULL_NOT_ALLOWED, COMMAND_INPUT_ENUM_MISMATCH, COMMAND_INPUT_CONSTRAINT_FAILED, COMMAND_INPUT_ADDITIONAL_PROPERTY, SCHEMA_DEFINITION_INVALID
- Path format: RFC 6901 JSON Pointer
- Golden capability fixture changes: all command input_schema now generated from SchemaNode::to_json_schema(); x-schema-version present
- Dependency changes: engine_schema depends on engine_validation; engine_command depends on engine_schema; tactical_rpg_tools depends on engine_schema; engine_agent_host depends on engine_schema

## Rollback evidence

Rollback restores the previous hand-authored descriptor JSON and Serde-only validation path as one coherent change. The schema authority is the single validation mode; there is no dual-mode fallback.

## Closure checklist

- [x] All Issue #9 acceptance criteria are checked by executable tests.
- [x] `docs/reference/COMMAND-SCHEMA-CONTRACT.md` describes implemented behavior.
- [x] Targeted validation is recorded with exact results.
- [ ] Full clean-head CI is green.
- [ ] Backlog status is changed from In Progress to Done.
- [ ] Draft PR is marked Ready for review only after clean-head CI.
- [ ] PR is squash merged.
- [ ] Issue #9 is closed as completed.
- [ ] Obsolete `status:ready` label is removed.
